# Historical audits

These notes document decisions made while recovering the original handover.
Paths beginning with `algorithm_benchmark/`, `rq6_clustering_pipeline/`, or an
absolute Windows drive refer to the pre-publication workspace. Use
`docs/MIGRATION_MAP.md` to locate their organized equivalents.

Audit statements describe the code/results available at the date of each
audit. The current scientific scope is defined by
`docs/MANUSCRIPT_ALIGNMENT.md`.
